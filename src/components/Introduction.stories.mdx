import {Meta} from '@storybook/addon-docs';

<Meta title="Introduction"/>

# コンポーネント設計方針

* コンポーネントライブラリを使用し、０からは作成しない
* コンポーネントライブラリで使用する値をそのまま使用しない
* ドメインに依存するコンポーネントを作らない
  * 依存するコンポーネントは domain の実装である infra 内に作成した components ディレクトリに作成
